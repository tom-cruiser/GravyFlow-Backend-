{
	# Only on gravyflow-network (docker-compose.yml pins this address), so
	# app containers on gravyflow-apps can't reach the unauthenticated API.
	# The API replaces this whole config via /load on startup.
	admin 172.20.255.250:2019
}

:80 {
	respond "GravyFlow Caddy is running" 200
}
